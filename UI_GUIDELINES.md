# UI_GUIDELINES

## 产品风格

项目采用企业级 SaaS 风格，界面应克制、清晰、可扫描，优先服务高频工作流与信息管理。

设计目标：

- 专业
- 稳定
- 清晰
- 高效
- 适合桌面端生产力场景

## 色彩规范

- 主背景：白色
- 页面辅助背景：浅灰或极浅蓝灰
- 主色：蓝色，用于主要按钮、选中态、链接和关键操作
- AI 强调色：紫色，用于 AI 生成、智能分析、模型能力等语义
- 成功：绿色
- 警告：黄色
- 危险：红色
- 文本：深灰或近黑
- 次级文本：中性灰
- 边框：浅灰

避免大面积使用高饱和渐变、过重阴影或营销型视觉元素。

## AppShell 三栏布局

桌面端采用 AppShell 三栏布局：

```text
Left Sidebar | Main Content | Right Context Panel
```

### Left Sidebar

- 用于主导航。
- 展示项目级入口、模块入口或工作区切换。
- 保持宽度稳定，不随内容跳动。

### Main Content

- 承载当前页面的主要工作流。
- 优先使用清晰的标题、Toolbar、Table、Card List 或 Detail View。
- 页面信息层级应适合快速扫描。

### Right Context Panel

- 展示当前项目、商品、素材、生成任务或 AI 参数的上下文信息。
- 可用于属性编辑、生成设置、预览信息或辅助操作。

## Design System 使用规则

- 优先复用现有 Design System 组件。
- 不为单个页面临时创造风格不一致的新组件。
- 新组件必须符合现有间距、字号、圆角、边框、阴影与状态规范。
- 同类操作使用同类组件表达。
- 主要操作使用主按钮，次要操作使用次按钮或图标按钮，危险操作使用危险样式。
- AI 相关操作可以使用紫色语义，但不得破坏整体 SaaS 视觉一致性。

## 组件规范

### Card

- 用于承载项目、商品、媒体或生成结果等重复内容。
- 内容结构应稳定，避免因 hover、loading 或长文本导致布局跳动。
- 不把页面大区块都做成浮动卡片。
- 卡片内必须有清晰标题、关键元数据和主要操作入口。

### Toolbar

- 用于承载筛选、搜索、排序、视图切换和主要操作。
- 操作顺序从高频到低频。
- 主要操作靠右或放在页面固定习惯位置。
- Toolbar 不应堆叠过多解释性文案。

### Table

- 用于展示可比较的结构化数据。
- 表头清晰，列宽稳定。
- 操作列保持一致。
- 空数据时显示 EmptyState。
- 加载时显示 Loading 状态，不显示半成品数据。

### Dialog

- 用于创建、编辑、确认危险操作或展示短流程。
- 标题必须明确表达动作。
- 主操作和取消操作位置稳定。
- 危险操作必须有明确的危险样式。
- 不在 Dialog 中塞入复杂多页面流程。

### EmptyState

- 用于无项目、无商品、无媒体、无生成结果等场景。
- 文案简洁，说明当前状态。
- 如有明确下一步，提供一个主要操作。
- 不使用营销式大段说明。

### Loading

- 用于接口请求、媒体上传、AI 分析、AI 生成等等待状态。
- Loading 应稳定占位，避免页面跳动。
- AI 生成可使用紫色语义提示，但保持克制。

### Error

- 错误状态必须说明发生了什么。
- 能恢复的错误应提供重试入口。
- 表单错误应尽量靠近对应字段。
- API 错误不直接暴露底层堆栈。

## 响应式范围

当前阶段暂不做移动端，仅支持桌面端。

桌面端优先级：

- 常规笔记本宽度
- 大屏桌面宽度
- 三栏布局可用性
- 表格、素材网格与右侧上下文面板的稳定展示

后续如进入移动端适配，应单独立项，不在普通功能 Task 中顺手处理。

