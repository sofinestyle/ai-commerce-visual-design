# CODEX_RULES

## AI 接力开发规则

所有 Codex、Cursor 或其它 AI 工具继续开发本项目时，必须遵守以下规则。

## 执行范围

- 一次只执行一个 Task。
- 不跨模块修改；除非当前 Task 明确要求，否则只修改与任务直接相关的文件。
- 修改前先确认范围，明确本次会改哪些模块、不会改哪些模块。
- 不新增与当前 Task 无关的功能、抽象、依赖或 UI 调整。

## Git 与依赖

- 不主动 commit。
- 不主动创建 tag。
- 不主动升级依赖。
- 不主动重装或替换包管理器。
- 如确需新增或升级依赖，必须先停止并汇报原因、影响范围与风险。

## 验证要求

- 每次修改后必须运行：

```bash
npm run lint
```

- 如果 lint 无法运行，必须明确说明原因。
- 如果 lint 失败，必须汇报失败信息，并在当前 Task 范围内优先修复。

## 汇报要求

每次完成后必须汇报：

- 修改了哪些文件
- 每个文件的主要变化
- 验证结果，至少包括 `npm run lint`
- 下一步建议

## 不确定问题处理

遇到以下情况必须停止并汇报，不得擅自推进：

- 需求与现有架构冲突
- 数据模型不明确
- API 入参或返回格式不明确
- 需要跨模块改动
- 需要新增依赖
- 需要修改数据库 schema 或迁移
- 可能破坏已有功能
- 无法确认用户真实意图

## 风险提示规范

继续使用绿色 / 黄色 / 红色风险提示：

- 绿色：低风险，局部修改，不影响数据结构和主流程。
- 黄色：中风险，涉及接口、服务逻辑、状态流转或多个文件。
- 红色：高风险，涉及数据库 schema、认证、权限、支付、外部 AI 真实调用、依赖升级或潜在破坏性变更。

AI 在汇报方案或结果时，应主动标注当前 Task 的风险等级。

